# ADR-NNNN: <short title>

- **Status:** Proposed <!-- Proposed | Accepted | Rejected | Superseded by ADR-NNNN | Deprecated -->
- **Date:** YYYY-MM-DD
- **Deciders:** <names / roles>
- **Related:** <other ADR numbers, mockup, migration unit IDs>
- **TL;DR:** <the decision, restated in one plain-language sentence — no jargon a skim reader would need to look up>
- **Quality goals:** <which Quality goals from docs/vision.md this decision serves, and how. Write "none" if it serves none.>

## Context

What problem are we deciding on? Forces, constraints, requirements. Link evidence
(the mockup, a migration unit, a benchmark).

## Decision

The choice we are making, stated plainly in one or two sentences.

## Options considered

> Optional (grill-with-docs) — delete this section if there wasn't a genuine rejected
> alternative worth remembering. Don't fill it in just because the template has it.

| Option | Pros | Cons |
|--------|------|------|
| A (chosen) |  |  |
| B |  |  |

## Consequences

> Optional (grill-with-docs) — delete this section if the effects are obvious. Keep it only
> for non-obvious downstream effects a future reader would otherwise have to rediscover.

What becomes easier, what becomes harder, what we are now committed to, and any
follow-up work this creates.

## Open questions

> While Proposed: list what the team must answer to finalize this decision.
> At Accepted time (ADR-0037): if every item is resolved inline, delete this section. If
> genuinely open items remain, rename the heading to `## Open questions (deferred, not
> blocking)` and keep them — don't collapse real open items into a summary line, and don't
> leave a plain `## Open questions` heading on an Accepted ADR (it reads as still-blocking).

- [ ] <what the team must answer to finalize this decision>
